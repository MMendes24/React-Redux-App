import React from 'react'
import { connect } from 'react-redux'

const LiturgicalCalendar = (props) => {
    return (
        <h1>Liturgical Calendar</h1>
    )
}

export default connect(null, {})(LiturgicalCalendar)